# kattack
kattack lets you define HTTP load tests as Kubernetes custom resources and execute them inside the cluster. It turns ephemeral load-test logs into structured Kubernetes status that can be used for automation and release gates.
